services:
  redis:
    image: redis:7-alpine
    container_name: caching
    ports:
      - "6666:6379"
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data

volumes:
  redis_data: